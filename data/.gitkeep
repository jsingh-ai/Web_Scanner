# This directory holds runtime data at deploy time: the SQLite database and saved
# screenshots. Its contents are git-ignored (see ../.gitignore); this file only keeps
# the empty directory in version control.
