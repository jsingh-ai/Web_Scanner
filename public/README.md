# public/

Static frontend assets served by the dashboard — plain HTML, vanilla ES-module JS,
and CSS. **No build step and no CDN** (see [../docs/BUILD-RULES.md](../docs/BUILD-RULES.md)
rules 22 & 26). Populated in Phase 6.

Planned: `index.html` (status page), `settings.html` (⚙ app management), `app.js`,
`styles.css`.
